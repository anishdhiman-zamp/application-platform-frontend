export enum DAY_TYPES {
  TODAY = 'today',
  YESTERDAY = 'yesterday',
  TOMORROW = 'tomorrow',
}

export enum OFFSET_TYPES {
  LATER = 'later',
  AFTER = 'after',
}

export enum DIRECTION_TYPES {
  NEXT = 'next',
  LAST = 'last',
}
